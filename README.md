# Sqltask3📊 SQL Aggregate Functions with GROUP BY and HAVING
📌 Overview
This project demonstrates how to use SQL aggregate functions on numeric columns. It also shows how to group data using GROUP BY and filter grouped results using HAVING.
🛠️ Features
Use of SUM(), AVG(), COUNT(), MIN(), MAX()
Categorizing data using GROUP BY
Filtering grouped data using HAVING
✅ Outcome
This project helps in summarizing and analyzing numeric data effectively using SQL.
